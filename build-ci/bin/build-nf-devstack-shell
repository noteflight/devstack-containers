#!/bin/bash

# Get the directory of this script
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

. ${DIR}/build-common

build_container \
    ${DIR}/../../containers/nf-devstack-shell \
    nf-devstack-shell \
    nf-devstack-shell
